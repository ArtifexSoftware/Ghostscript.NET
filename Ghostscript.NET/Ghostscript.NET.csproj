<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFrameworks>netstandard2.1;netcoreapp3.1;net5.0;net4;net461;net47;net471;net472</TargetFrameworks>
	</PropertyGroup>

	<PropertyGroup>
		<AllowUnsafeBlocks>true</AllowUnsafeBlocks>
		<AssemblyName>Ghostscript.Net.Core</AssemblyName>
		<Company>Habjan</Company>
		<Copyright>Copyright © 2013-2021 by Josip Habjan (habjan@gmail.com). All Rights Reserved.</Copyright>
		<Description>Managed wrapper library around the Ghostscript native library (32-bit &amp; 64-bit), an interpreter for the PostScript language, PDF, related software and documentation.</Description>
		<Authors>Habjan</Authors>
		<Product>Ghostscript.NET</Product>
		<Version>1.3.0</Version>
		<FileVersion>1.3.0</FileVersion>
		<AssemblyVersion>1.3.0</AssemblyVersion>
		<PackageLicenseExpression>AGPL-3.0-only</PackageLicenseExpression>
		<PackageProjectUrl>https://github.com/jhabjan/Ghostscript.NET</PackageProjectUrl>
		<RepositoryUrl>https://github.com/jhabjan/Ghostscript.NET.git</RepositoryUrl>
		<RepositoryType>git</RepositoryType>
		<PackageId>Ghostscript.NET</PackageId>
		<PackageIcon>icon.png</PackageIcon>
		<PackageTags>.NET ghostscript pdf conversion image</PackageTags>
		<PackageReleaseNotes>Added compatibility for .NET Standard, .NET Core and .NET 5.</PackageReleaseNotes>
		<PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
		<GeneratePackageOnBuild>true</GeneratePackageOnBuild>
		<PublishRepositoryUrl>true</PublishRepositoryUrl>
		<EmbedUntrackedSources>true</EmbedUntrackedSources>
		<PublishRepositoryUrl>true</PublishRepositoryUrl>
		<EmbedUntrackedSources>true</EmbedUntrackedSources>
		<IncludeSymbols>true</IncludeSymbols>
		<SymbolPackageFormat>snupkg</SymbolPackageFormat>
		<ContinuousIntegrationBuild>true</ContinuousIntegrationBuild>
	</PropertyGroup>

	<ItemGroup>

	</ItemGroup>

	<ItemGroup Condition=" '$(TargetFramework)' == 'netstandard2.1' or '$(TargetFramework)' == 'netcoreapp3.1'  or '$(TargetFramework)' == 'net5.0' ">
		<PackageReference Include="Microsoft.Win32.Registry"
						  Version="5.0.0" />
		<PackageReference Include="System.Drawing.Common"
						  Version="5.0.2" />
		<PackageReference Include="System.Drawing.Primitives"
						  Version="4.3.0" />
	</ItemGroup>

	<ItemGroup>
		<None Include="..\icon.png">
			<Pack>True</Pack>
			<PackagePath></PackagePath>
		</None>
	</ItemGroup>
</Project>